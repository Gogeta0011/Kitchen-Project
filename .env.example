# Copy to .env and fill in. The .env file is gitignored (never commit secrets).
#
# Local dev: Postgres in Docker (see CLAUDE.md for the `docker run` command).
DATABASE_URL="postgresql://kitchen:kitchen_dev_pw@localhost:5432/kitchen?schema=public"
#
# Railway production: Railway auto-injects DATABASE_URL when you add a Postgres
# service — no need to set it manually there.
