# Legacy docs — historical only ⚠️

These documents describe the **old** stack (SQLite via sql.js, `web.config`
connection strings, SQL Server setup, ports 3000) **before** the project was
migrated to PostgreSQL + Prisma. They are kept for history and **do not reflect
how the app works now**.

For current setup, schema, and deployment, see the repo root:
[`README.md`](../../README.md) · [`docs/SCHEMA.md`](../SCHEMA.md) ·
[`NEXT_STEPS.md`](../../NEXT_STEPS.md) · [`CLAUDE.md`](../../CLAUDE.md).
